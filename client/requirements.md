## Packages
lucide-react | Used for beautiful and standard UI icons
clsx | Utility for constructing className strings conditionally
tailwind-merge | Utility to merge Tailwind classes

## Notes
The application implements optimistic UI updates for the meal toggle functionality to ensure a snappy, premium user experience.
Data fetching relies on the TanStack Query caching layer with proper invalidation.
